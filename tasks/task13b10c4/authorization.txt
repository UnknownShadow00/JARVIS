Task 13B10C4 authorization: change exactly one component family — the deterministic request
classifier / deterministic primary-action extraction / deterministic proposal guard — plus the
TEST-ONLY harness and evidence.

Frozen and byte-identical from Task 13B10C3: the operational provenance lock, the response-source
policy, the response safety gate, the response-need classifier, the lane policy, the turn glue, the
canonicalization map, the provenance ledger, the tool schemas, the dispatcher, the confirmation
policy, the tool-result trust rules, the operational response templates, the old detector, the
persona, the model and the base collector.

Production JARVIS and Hermes source/configuration: no changes. Hermes remains disabled in
production. No real side effects. No model download or substitution.

IMMUTABLE RULE (unchanged): for every OPERATIONAL turn, raw model prose is never user-visible.
Allowed operational sources remain exactly LEDGER, TOOL_SUCCESS, TOOL_ERROR, CONFIRMATION,
AMBIGUITY, MISSING_CONTEXT, DECLARATIVE_ACK, UNVERIFIED_STATUS, CAPABILITY_UNAVAILABLE.
SAFE_RAW is not added to operational turns.
