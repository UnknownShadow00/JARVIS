# ============================================================
# JARVIS — Python Dependencies (full desktop install)
# Install: pip install -r requirements.txt
# Server/Docker only needs: pip install -r requirements-core.txt
# ============================================================

-r requirements-core.txt
-r requirements-desktop.txt
